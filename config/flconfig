# Which scheme should we use?
# default scheme = Alternative

# Which theme should we use?
# default theme = Alternative # loaded by Alternative scheme automatically
# default theme = KDE1
# default theme = classic
# default theme = essai
# default theme = motif
# default theme = windows
