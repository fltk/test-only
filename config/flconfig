# FLTK toolkit configuration file

[default]
  # include mouse wheel configuration

  include wheel.conf


  [default/scheme]
    # Which scheme should we use?  (Loads the themes too)
    # FLTK comes with:
    #   Alternative, Alt2, Classic, Essai, KDE1, Motif, OldMotif, Windows
    # (scheme info can be put directly in this file instead of using include)

    include schemes/Essai
