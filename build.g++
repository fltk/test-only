#!/bin/sh

# When building on Linux, it is a good idea to user build.gcc instead.
# using build.g++ will link in libstdc++, which is a good way to make
# your application incompatible with other than your distribution.

autoconf

CC=gcc
CXX=g++
export CC CXX

./configure $*

make clean all
